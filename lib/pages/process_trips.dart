import 'package:flutter/material.dart';

class ProcessTrips extends StatelessWidget {
  const ProcessTrips({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}